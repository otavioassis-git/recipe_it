import 'package:flutter/cupertino.dart';

class RecipesList extends StatelessWidget {
  const RecipesList({super.key});

  @override
  Widget build(BuildContext context) {
    return Text('Recipes list');
  }
}
